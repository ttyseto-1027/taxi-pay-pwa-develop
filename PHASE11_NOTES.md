# Phase 11 競合・アーカイブ・削除管理

作業対象はDevelop環境のみ。本番環境へは反映しない。

## 現在確認済みの基盤
- 同一IDの内容差異を競合として検出
- 同一勤務日の別データを競合として検出
- 一方で削除済み、他方でデータありの状態を競合として検出
- 採用しなかったデータを `dataArchive` へ退避
- 競合解決結果を `conflictHistory` へ記録
- 通常削除時に退避データと tombstone を作成
- 退避データの完全削除後は給与内容を残さず `deletionHistory` の監査情報だけ保持

## 今回追加した安全回帰
`tests/phase11-safety-v14.cjs`

確認項目:
1. 通常削除で元データを退避し tombstone を作成する
2. 削除済み対データありを競合として扱う
3. 削除状態採用時も残存データを先に退避する
4. データあり採用時は有効データへ戻せる
5. 完全削除後の監査記録に給与内容を残さない
6. 選択削除が他の退避データへ影響しない

## 残件
- 現在の退避一覧UIには「復元できる」と説明がある一方、直接復元する操作ボタンがまだない。
- 退避データから有効データへ戻す際は、既存の同一ID・同一勤務日データとの再競合を必ず確認し、無条件上書きしない設計にする。
- UI実装後、全回帰テストを再実行する。
